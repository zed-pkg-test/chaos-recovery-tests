[project]
name = "zed-pkg-test-chaos-recovery-tests"
version = "0.2.0"
description = "Deterministic fault injection and black-box interactive crash recovery for host and OCI runtimes."
requires-python = ">=3.11"
dependencies = []

[tool.deep-tests]
suite = "chaos"
deterministic = true
network_default = "disabled"
