[project]
name = "zed-pkg-test-chaos-recovery-tests"
version = "0.3.0"
description = "Black-box interactive package lifecycle and crash recovery for host and OCI runtimes."
requires-python = ">=3.11"
dependencies = []

[tool.deep-tests]
suite = "chaos"
deterministic = true
network_default = "disabled"
